// Import reactDOM and routes
import ReactDOM from "/react-dom";
import routes from "./config/routes";

// Render the router to the "app" div in index.html
ReactDOM.render(routes, document.getElementById("app"));